// The for loop may be used to increment by 2's, allowing to add just odd or just even numbers to the array.

// Example
var ourArray = [];

for (var i = 0; i < 10; i += 2) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.
for (var i = 1; i < 10; i +=2) {
  myArray.push(i);
}
