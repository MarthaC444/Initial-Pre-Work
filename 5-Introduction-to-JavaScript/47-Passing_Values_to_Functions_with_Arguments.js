// Values that are passed to functions are called arguments, the placholder they are passed though is called a parameter.

// Example
function ourFunctionWithArgs(a, b) {
    console.log(a - b);
  }
  ourFunctionWithArgs(10, 5); // Outputs 5
  
  // Only change code below this line.
  
  function functionWithArgs(c, d) {
    console.log(c + d);
  }
  
  functionWithArgs(2, 5);