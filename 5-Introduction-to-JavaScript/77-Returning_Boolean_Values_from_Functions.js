// Comparison operators return true or false. Sometimes it may make sense to return the boolean value instead of doing an if else statement. 

function isLess(a, b) {
    // Fix this code
    return a < b;
  }
  
  // Change these values to test
  isLess(10, 15);