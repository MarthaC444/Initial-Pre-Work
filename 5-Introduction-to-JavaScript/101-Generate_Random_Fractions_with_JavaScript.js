// Math.random() will return a value between 0 and 1. Inclusive of 0 and exclusive of 1.


function randomFraction() {

    // Only change code below this line.
  
    return Math.random();
  
    // Only change code above this line.
  }